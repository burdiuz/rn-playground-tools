/*
    So, basically this tool will allow to add new snippets from the code currently present in code editor.
    It will ask user if they want to put into snippet whole file or selected code.
    Then it will parse code to find ${} and all found will be listed as parameters so users may fill them in.
    Any parameter may be marked as not-a-parameter and its felds will disappear.
    After entering fields user adds Snippet name and description and it's saved.
*/
